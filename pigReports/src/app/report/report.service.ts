import { Inject, inject, Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
  
export class Report {
  // name: string;
  // phoneNum: number;
  // breed: string;
  // Pid: number;
  // location: string;
  // longitude: number;
  // latitude: number;
  // extraNotes: string;
  // date: number;
  // time: number;
  // status: boolean;

  // constructor(@Inject('n') n: string, @Inject('pN') pN: number, b:string, id: number, l: string, long: number, la: number, extra: string, s:boolean) {
  //   this.name = n;
  //   this.phoneNum = pN;
    
  //   this.breed = b;
  //   this.Pid = id;

  //   this.location = l;
  //   this.longitude = long;
  //   this.latitude = la;
  //   this.extraNotes = extra;

  //   this.date = new Date().getTime();
  //   this.time = new Date().getTime();

  //   this.status = s
  //  }

  // reports = [];

  // addNewReport(name: string, phoneNum: number, breed: string, Pid: number, location: string, longitude: number, latitude: number, extraNotes: string, date: number, time: number, status: boolean) {
    
  // }
  
}
